# Copy to .env and fill in. .env is git-ignored.

# Storage backend: memory | supabase
STORAGE_BACKEND=memory

# Supabase (required only when STORAGE_BACKEND=supabase)
SUPABASE_URL=
SUPABASE_KEY=

# P4 generative layer (team credits are on OpenAI)
OPENAI_API_KEY=
